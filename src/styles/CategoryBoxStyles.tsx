import { StyleSheet } from "react-native";

const CategoryBoxesStyles = StyleSheet.create({
    suppPillCount: {
        color: "black",
        fontSize: 20,
        textAlign: "center",
        marginTop: 5,
        backgroundColor: "white",
        padding: 1,
        borderRadius: 12,
        overflow: "hidden"
    },
});

export default CategoryBoxesStyles;
